<%= column_chart completed_setup_by_month_path, @options.merge({title: 'completed SETUP by month', stacked: true}) %>
<%= area_chart average_setup_duration_by_month_path, @options.merge({title: 'average SETUP duration by month', ytitle: 'hours'}) %>
